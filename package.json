{
  "scripts": {
    "build": "npm install --omit=dev && npm run build",
    "start": "node api/send-email.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "nodemailer": "^6.9.16"
  }
}
